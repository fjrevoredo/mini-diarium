# TODO

Open tasks and planned improvements. For full context and implementation notes on the original tasks, see [TODO_EXTRA.md](TODO_EXTRA.md).

TODO entry format:

- `- [ ] **TODO-XXXX: Task title** — concise requirement-style description with scope and constraints`
- Every top-level checkbox item must carry a unique `TODO-XXXX` ID (4-digit zero-padded, e.g. `TODO-0001`)
- Indented sub-items are free-form and do **not** carry IDs
- Write items as requirements/acceptance criteria (what must be true), not implementation plans (how to build it)
- Keep implementation details minimal in TODO entries; move deep implementation notes to [TODO_EXTRA.md](TODO_EXTRA.md) when needed
- New IDs are assigned by reading the `Latest TODO ID` marker near the top of this file and incrementing — never reuse IDs
- After creating a new TODO, update the `Latest TODO ID` marker to reflect the new highest ID
- Use the `todo-manager` skill (`.agents/skills/todo-manager/`) for creation, tracking, archival, and validation

**Latest TODO ID: TODO-0086** — next new TODO should be TODO-0087

---

## High Priority

---

- [ ] **TODO-0081: Spell check not functioning on Linux/WebKitGTK (#227)** — the "Enable spellcheck" preference (Preferences → Writing) silently does nothing on Linux (reported on Debian 13 / Flatpak, v0.6.2). Root cause: the implementation only sets the HTML `spellcheck` attribute on the editor DOM (`DiaryEditor.tsx:257`, `TitleEditor.tsx:47`), which WebKitGTK — Tauri's Linux WebView — ignores unless the WebView-level spell-checking setting is also enabled and Hunspell/enchant dictionaries are reachable. macOS (WKWebView) and Windows (WebView2) honor the attribute via OS-native checkers, so only Linux is affected; the Flatpak sandbox further restricts dictionary access. Acceptance: with the preference on, misspelled words are underlined on Linux with system dictionaries installed, and the toggle still disables them cleanly; document any Flatpak finish-args/runtime-extension changes needed for dictionary access.

- [ ] **TODO-0008: Cursor height too tall after Shift+Enter on macOS (#118)** — the text caret (cursor) height becomes extra long starting on the second line, but only when inserting a soft line break with Shift+Enter (Enter alone works fine); macOS-only (v0.4.20); likely a `line-height` or `font-family` mismatch in ProseMirror's `<br>` handling introduced with new editor fonts in v0.4.20; audit soft-break styling in `src/styles/editor.css` and the `--editor-font-family` / `--editor-font-size` CSS custom properties
---

## Website Priority

- [ ] **TODO-0011: Website SEO/GEO follow-up backlog** — remaining implementation items from the 2026 website SEO/GEO pass. **The `transition: all` fix is done** (2026-07-16 audit: no `transition: all` remains in `website/css/style.css` — all transitions already use explicit property lists); only the Cloudflare item below remains, and it is a CDN dashboard setting with no repo code change.
  - [ ] **Resolve Cloudflare-injected robots.txt Content-Signal directive** — Cloudflare automatically appends `Content-Signal: search=yes,ai-train=no` to the live robots.txt at the CDN layer; Lighthouse's robots.txt parser flags this as invalid (not part of RFC 9309), costing 8 SEO points (score 92 → 100); the repo `website/robots.txt` is clean — this is a Cloudflare dashboard setting (REPORT.md FIX 2.1)
    - **Fix:** in the Cloudflare dashboard → Security → Bots → Crawler Hints, disable "Content Signals" injection or switch to the HTTP-header equivalent (`X-Robots-Tag: ai-train=no`) if available. No code change in the repo is needed — AI bot blocking is already handled by explicit `User-agent` blocks in the live robots.txt.

---

## Medium Priority

- [ ] **TODO-0050: Update dep-update skills and CI for Nix npmDepsHash** — after PR #159 (Nix flake) merges, update `sync-lockfiles` and `apply-dependency-prs` skills to include the `npmDepsHash` refresh step in `nix/package.nix`, and consider adding a path-filtered CI job to catch stale hashes from contributors who bypass the skills; see [TODO-0050-01](TODO_EXTRA.md#todo-0050-01-skill-and-ci-updates-for-nix-npmdepshash-maintenance) for exact changes. **Part 2 (apply-dependency-prs update) completed 2026-06-29 during the runbook refactor; Part 3 (CI) completed — `.github/workflows/nix.yml` is a path-filtered job (`package-lock.json`/`nix/**`/`flake.*`) that runs `nix build .#default` to verify `npmDepsHash` and even auto-refreshes the hash on push. Only Part 1 (add the `npmDepsHash` refresh step to the `sync-lockfiles` skill) remains.**

- [ ] **TODO-0062: Add Statistics, Import, Export to the header overflow menu** — extend the `HeaderMoreMenu` component from TODO-0061 with three more items, each wired to its existing overlay state setter (`setIsStatsOpen`, `setIsImportOpen`, `setIsExportOpen`); mirror the `menu.rs` "lockable" disabled-while-locked state for these three items (Preferences stays always-enabled); update `OnboardingOverlay.tsx`'s Import tour step, which currently points its arrow off-screen at the native OS menu bar, to target the new in-app control instead; see [TODO-0062-01](TODO_EXTRA.md#todo-0062-01-statistics-import-export-menu-items) for exact file/line references. **Handled behind the `inAppMenu` runtime feature flag (2026-07-11):** the three items landed on `master` in `HeaderMoreMenu.tsx` (new migration-free store `src/state/feature-flags.ts`, toggle in Preferences → Advanced → Experimental), defaulting **off** so users don't see them until the whole menu migration completes. The gate wraps the **entire `⋮` menu** at the `Header.tsx` render site (`<Show when={isFeatureEnabled('inAppMenu')}><HeaderMoreMenu /></Show>`) — trigger and all items, Preferences included — not each item; `HeaderMoreMenu` itself is flag-agnostic. Preferences stays reachable via the native OS menu while the flag is off, so there is no lock-out. The disabled-while-locked mirroring was intentionally skipped (the Header only mounts in the unlocked session, so items are always reachable/enabled — unlike `menu.rs`). `e2e/specs/header-actions.spec.ts` now enables the flag via the new `setFeatureFlag` helper (localStorage seed + reload) before driving the menu. **Onboarding retarget done (2026-07-19):** `OnboardingOverlay.tsx`'s Import tour step now points at the in-app `⋮` control (`targetSelector: '[data-tour-target="import"]'` on the `HeaderMoreMenu` trigger) when `inAppMenu` is on, and falls back to the existing native-menu-bar `edgeHint` while the flag is off (element absent → `measure()` fallthrough). **Component-level coverage added (2026-07-20):** the three flag-gated items now have direct Vitest coverage in `Header.test.tsx` — an `it.each` proving `⋮` → Statistics/Import/Export each flips its `state/ui` overlay signal (`isStatsOpen`/`isImportOpen`/`isExportOpen`) when `inAppMenu` is on — plus E2E `⋮` → overlay "opens" smoke checks in `header-actions.spec.ts` (see TODO-0064). **Still pending before un-gating:** remove the flag gate as part of the TODO-0065 graduation (native-menu removal is approval-gated) — unchanged, this remains the only open step.
- [x] **TODO-0063: Add in-app day-navigation and go-to-date controls to the Header** — add `◀`/`▶` day-navigation buttons flanking the Header date title, reusing the existing `navigatePreviousDay`/`navigateNextDay` helpers (extract the logic currently inlined in `MainLayout.tsx`'s `menu-navigate-previous-day`/`next-day` listeners into a shared function called by both the listener and the new buttons); make the date title itself clickable to open the existing `GoToDateOverlay`; audit Header layout at the 800×660 E2E viewport width for overflow risk (`src/CLAUDE.md` gotcha #4, `e2e/CLAUDE.md` gotchas #2–3); see [TODO-0063-01](TODO_EXTRA.md#todo-0063-01-day-navigation-and-go-to-date-controls) for exact file/line references
- [ ] **TODO-0064: E2E coverage for in-app-reachable actions (right-sized)** — main-paths-only WebdriverIO coverage for the Header actions that become WebView-reachable as TODO-0061–0063 land, kept as shallow "overlay opens" smoke checks (not per-item feature suites — deep import/export behavior stays in Rust + Vitest). **Done:** shared auth/onboarding helper extraction (`e2e/specs/helpers.ts`) + `header-actions.spec.ts` covering `⋮` → Preferences (via TODO-0061), plus the day-nav (`◀`/`▶` change the Header date title) and date-title → `GoToDateOverlay` checks (landed with TODO-0063). **Extended (2026-07-20):** the deferred `⋮` → Statistics/Import/Export "opens" smoke checks now landed in `header-actions.spec.ts` — a shared `assertMenuItemOpensOverlay` helper (also folds in the Preferences check to dodge the SonarCloud duplication gate) driven against new `stats-overlay`/`import-overlay`/`export-overlay` data-testids on the three `Dialog.Content`s. Fixed a latent WebView2-only bug in the shared `setFeatureFlag` helper while landing these — the flag reload used `browser.url('tauri://localhost')`, a no-op on msedgedriver/WebView2, so the `⋮` menu never rendered on Windows (green only on CI/WebKitGTK); switched to `browser.refresh()` (documented as `e2e/CLAUDE.md` gotcha #4). With that the TODO-0064-01 deferred list is empty (day-nav/go-to-date already noted under TODO-0063). See [TODO-0064-01](TODO_EXTRA.md#todo-0064-01-e2e-coverage-for-in-app-reachable-actions-right-sized) for exact scope
- [ ] **TODO-0072: Timeline display preferences — date format and preview visibility (#205)** — add two new user preferences scoped to the timeline view: (1) let users choose the date format displayed per timeline row, and (2) let users show or hide the entry text preview in the timeline; follow the existing preferences storage taxonomy (`docs/decisions/2026-05-settings-storage-taxonomy.md`) to decide where each setting belongs (likely `localStorage`, consistent with other display-only preferences)
- [x] **TODO-0076: Open-core M1 — extract `mini-diarium-core` crate (workspace split)** — introduce a repo-root virtual Cargo workspace and move the Tauri-free layer (`crypto/`, `auth/`, `db/`, `import/`, `export/`, `plugin/`, `backup.rs`, `config.rs`, and shared types) into `crates/mini-diarium-core/` with **no** `tauri` dependency; the app stays at `src-tauri/`, keeps `commands/*` and the OS shell, and gains a path dependency on the new crate; behavior-preserving code-move only (no logic changes). Must include the mandatory tooling migration a workspace split forces: CI workflows, coverage tooling (`cargo llvm-cov nextest` / `check-diff-coverage.mjs` / `codecov.yml`), `nix/package.nix`, benchmark manifests (`bench = false` on the new crate too), and the `--manifest-path src-tauri/Cargo.toml` references in the `CLAUDE.md` files/runbooks. Acceptance: `mini-diarium-core` compiles with zero `tauri::` references, app behaves identically, and the full backend/frontend suites, E2E, and coverage gate stay green. Depends on TODO-0075. See [OPEN_CORE_STRATEGY.md §8 + §10, M1](../OPEN_CORE_STRATEGY.md#10-open-core-extraction-roadmap). **Gap-filled 2026-07-22** ([review](../reports/2026-07-22-open-core-m1-m2-review.md) P2): the "no stale single-crate assumptions in docs" criterion was not fully met — `README.md` and `.claude/agents/test-failure-analyst.md` still pointed at the pre-workspace `src-tauri/target/` location. Both corrected to the workspace-root `target/`, plus a narrow regression guard (`scripts/check-stale-build-paths.js` → `bun run check:build-paths`, wired into `quick-check` and `pre-commit`).
- [x] **TODO-0077: Open-core M2 — stable `mini-diarium-core` façade API** — expose one deliberate public surface on the core crate (open/unlock, entry CRUD, search, import/export, auth-slot management), refactor `commands/*` to call only that façade, and seal everything else with `pub(crate)`/private; add `crates/mini-diarium-core/API.md` documenting that surface. Boundary enforced by module visibility plus code review for now; an automated `cargo-public-api` regression guard is a deferred follow-up (add once `minidiarium-plus` exists). Acceptance: app compiles and passes the full suite against the façade only, and `commands/*` no longer reference `db::queries::*` internals directly. Depends on TODO-0076. See [OPEN_CORE_STRATEGY.md §10, M2](../OPEN_CORE_STRATEGY.md#10-open-core-extraction-roadmap). **Gap-filled 2026-07-22** ([review](../reports/2026-07-22-open-core-m1-m2-review.md) P1+P2): the façade-only claim was not literally true — `peek_auth_slot_types` still opened its own `rusqlite::Connection` with bespoke SQL, leaving a second DB access path in the app. Moved behind the façade as `db::peek_auth_slot_types(path)` (core-owned `JournalPeek`/`AuthSlotPeek`, `open_connection` so the FK pragma holds, `get_db_setting_conn` for the pre-v6 `db_settings` semantics); the command is now a thin wrapper and `rusqlite` is gone from the app crate's dependencies. `API.md` was also extended from a symbol inventory into an honest **pre-1.0/internal** contract (compatibility status, MSRV/edition, error policy incl. the two `mapTauriError`-coupled phrases, secret handling, handle/transaction semantics, frozen serde fields, change rule). Module-size debt tracked separately as TODO-0080.
- [ ] **TODO-0078: Enable the Microsoft Store update pipeline** — `msstore-publish.yml` must actually publish a package update on a tagged release; today it fails at its secrets preflight on every release, silently (`release.yml` dispatches it with `continue-on-error: true`). Gated on maintainer Partner Center access — all steps are manual. Must be true when done: (1) an Azure AD (Entra) app registration with a client secret exists and is associated with the Partner Center account; (2) the five repo secrets `PARTNER_CENTER_TENANT_ID`, `PARTNER_CENTER_SELLER_ID`, `PARTNER_CENTER_CLIENT_ID`, `PARTNER_CENTER_CLIENT_SECRET`, and `MSSTORE_PRODUCT_ID` (= `9PJFTX44ZS43`) are set — the workflow enforces these names verbatim; (3) a dry run (`gh workflow run msstore-publish.yml --ref master --field tag=v0.6.2 --field dry_run=true`) succeeds — this is the **first real execution** of the `msstore reconfigure` + `msstore publish --noCommit` orchestration, which has never run and has two pre-documented failure modes (`msstore publish` may reject a standalone MSIX → fall back to the `msstore submission` sub-commands; the CLI is installed by display name → pin its `--id` if the winget match turns ambiguous); (4) `msstore submission status 9PJFTX44ZS43` shows a draft that was created and **not** committed. See [RELEASING.md → Microsoft Store (MSIX)](../RELEASING.md#microsoft-store-msix)

---

## Low Priority / Future
- [x] **TODO-0082: Open-core M3a — extract a `rusqlite`-free crypto/auth kernel** — create a small, Tauri-free and `rusqlite`-free kernel surface containing the established crypto primitives, master-key wrapping/unwrapping, and portable encrypted-format value types needed by another storage implementation; preserve current algorithms, secret-zeroization guarantees, and the sealed pre-1.0 façade. Do not add browser/WASM storage, network code, or a generic persistence framework. Acceptance: the kernel compiles without `rusqlite` in its dependency graph; focused crypto/auth and format tests exercise it directly; the desktop application retains unchanged behavior. Depends on TODO-0077 and should follow TODO-0080. See [OPEN_CORE_STRATEGY.md §10, M3](../OPEN_CORE_STRATEGY.md#10-open-core-extraction-roadmap). (Closed 2026-07-23: the kernel shipped as a new crate **`mini-diarium-crypto`** — named for what it is, superseding the doc's tentative "kernel". `crypto/{cipher,password}.rs` and the pure master-key wrapping `auth/{password,keypair,auto_key}.rs` (plus the `SecretBytes`/`KeypairFiles` value types) moved verbatim (`git mv`, byte-identical renames) into `crates/mini-diarium-crypto/`, which carries only the 10 crypto deps it names — **no `rusqlite`**. Acceptance gate met: `cargo tree -p mini-diarium-crypto` shows no `rusqlite` and `cargo build -p mini-diarium-crypto` succeeds standalone. `mini-diarium-core` now depends on it and re-exports `crypto`/`auth` (`pub use mini_diarium_crypto::crypto;` + a thin `auth/mod.rs` bridge keeping the sub-module paths `pub(crate)`), so the M2 façade, the app crate, and the benches see a byte-for-byte-unchanged surface — zero db-module or command-layer edits. The db-coupled pieces (`AuthMethodInfo`, `add_password_slot`/`add_keypair_slot`, and the encrypted-**row** format) intentionally stay in core; the row-format storage boundary is deferred to TODO-0083 (M3b). `argon2`+`aes-gcm` dropped from core; `x25519-dalek`/`hkdf`/`sha2` retained (still named by the db layer). Tooling rippled the M1 way: no-network check globs `crates/*/Cargo.toml`, `codecov.yml` + `check-diff-coverage.mjs` gate the new crate as backend, new `crates/mini-diarium-crypto/API.md`, docs updated (root + `src-tauri` `CLAUDE.md`, `OPEN_CORE_STRATEGY.md` §8/§10). Behavior-preserving: `cargo test --workspace` green — 538 tests across the three crates (35 crypto, 363 core, 140 app); clippy `--workspace --all-targets -D warnings` clean; `cargo fmt --all --check` clean.)
- [ ] **TODO-0083: Open-core M3b — make the encrypted-row format storage-injectable** — move the encrypted-row encoding/decoding and its portable operations behind the M3 kernel boundary, with the existing `rusqlite`/`DatabaseConnection` path retained as the desktop adapter; use the narrowest explicit boundary that supports another SQLite implementation, rather than introducing a general ORM or speculative trait hierarchy. Acceptance: kernel tests prove row-format round trips without `rusqlite`; desktop-path tests prove compatibility with existing databases and migrations; `cargo test --workspace` stays green. Depends on TODO-0082. See [OPEN_CORE_STRATEGY.md §10, M3](../OPEN_CORE_STRATEGY.md#10-open-core-extraction-roadmap).
- [ ] **TODO-0084: Open-core M4a — decide and record core-crate distribution** — make the maintainer decision between crates.io publication and a git dependency/submodule for `mini-diarium-core`, then record the rationale, versioning/compatibility stance, ownership, and publishing prerequisites in a short ADR. Do not publish or alter the app's release versioning until the ADR explicitly authorizes it. Acceptance: the ADR resolves the distribution choice and `API.md`/release guidance agree with it. Depends on TODO-0077. See [OPEN_CORE_STRATEGY.md §9 step 3 and §10, M4](../OPEN_CORE_STRATEGY.md#10-open-core-extraction-roadmap).
- [ ] **TODO-0085: Open-core M4b — record the contribution and relicensing posture** — update `CONTRIBUTING.md` to state that premium features belong in a separate product and repository, and record whether future relicensing of the open core is intended; introduce a CLA only if that recorded decision requires one. Acceptance: contributors can find the product boundary and contribution terms in-repo, the decision is captured in the M4 ADR or a linked ADR, and no CLA is added without an explicit relicensing rationale. Depends on TODO-0084. See [OPEN_CORE_STRATEGY.md §9 step 5 and §10, M4](../OPEN_CORE_STRATEGY.md#10-open-core-extraction-roadmap).
- [ ] **TODO-0086: Open-core M4c — establish the Mini Diarium name-protection posture** — audit the current trademark/name-protection status for "Mini Diarium" and "MiniDiarium+", choose and record the appropriate registration or defensive-use action with jurisdiction and owner, and preserve the distinction between trademark protection and the MIT code license. Acceptance: a dated decision record and evidence/reference to the chosen action exist; any filing or external legal step is explicitly maintainer-approved and tracked without storing confidential details in the repository. Depends on TODO-0084. See [OPEN_CORE_STRATEGY.md §10, M4](../OPEN_CORE_STRATEGY.md#10-open-core-extraction-roadmap).
- [ ] **TODO-0013: Text input extension point** — create a plugin/extension interface for alternative entry methods so official and user plugins can provide text input flows such as dictation, LLM-assisted drafting, and other future capture modes; define capability boundaries, permission model, and how plugins hand content into the editor without weakening the app's privacy guarantees
- [ ] **TODO-0014: Statistics extension point** — add a plugin/extension interface for writing statistics so official and user plugins can calculate custom metrics and surface them in the statistics UI; define the data contract, execution/sandbox constraints, and how custom statistics are registered and rendered without weakening the app's privacy-first local-only model
- [ ] **TODO-0017: Document keypair hex in JS heap** — `generate_keypair` returns `KeypairFiles` with `private_key_hex` as plain JSON so the frontend can write it to a file; add a comment on the struct in `crates/mini-diarium-crypto/src/auth/mod.rs` (where `KeypairFiles` now lives, post-M3a) or `crates/mini-diarium-crypto/src/auth/keypair.rs` (`generate_keypair`) noting this is an accepted design tradeoff and that the private key briefly exists in the JS heap before the file is written
- [ ] **TODO-0018: Sync tool integration** — allow users to point their journal directory at a folder managed by Dropbox, Google Drive, Syncthing, or similar tools; the app should detect when `diary.db` is modified externally while locked (file-system watcher or mtime check on unlock) and prompt the user to reload; document the supported workflow in the UI and guard against opening a partially-synced (in-progress) file; note that the app never initiates any network calls — sync is entirely delegated to the external tool
- [ ] **TODO-0038: Remove legacy `require_all_auth` config migration** — once the release boundary is agreed, remove the legacy `JournalConfig.require_all_auth` field, its migration function `migrate_require_all_auth_to_db`, and all call sites from backend and frontend; the DB-settings-backed implementation that replaced it stays untouched; requires maintainer approval before execution; see TODO-0038-01 for full steps
- [ ] **TODO-0065: Remove redundant native menu items once in-app equivalents ship** — after TODO-0061–0064 ship, remove the now-duplicated native menu entries from `menu.rs`: Previous/Next Day, Go to Date, Statistics, Import, Export; preserve their keyboard accelerators by converting them from native-menu-triggered to JS-level `keydown` listeners (same pattern as the existing `Cmd/Ctrl+F` search shortcut handled in `MainLayout.handleSearchShortcut`, not through the native menu) so power users keep `CmdOrCtrl+[`/`]`/`G` etc.; do **not** remove Quit, Services/Hide/Show All, Window management, the macOS Edit menu (Undo/Redo/Cut/Copy/Paste/Select All), or the Preferences native menu item (macOS convention expects `Cmd+,` under the App menu regardless of in-app access); requires maintainer approval before execution (same gate pattern as TODO-0038) since it changes native menu structure and removes a fallback access path; explicitly blocked on TODO-0061–0064; see [TODO-0065-01](TODO_EXTRA.md#todo-0065-01-native-menu-removal-scope) for exact removal scope
- [ ] **TODO-0039: Re-evaluate `glib` Dependabot alert when Tauri upgrades webkit2gtk bindings** — Dependabot alert #6 (`glib 0.18.5`, medium) was dismissed as a tolerated risk: the vulnerability is in `glib::VariantStrIter` (Linux-only, UB via unsound iterator impl), the app has zero direct `glib` usage, and upgrading requires `gtk 0.20` + `webkit2gtk 2.1.x` Rust bindings that do not yet exist in a Tauri-compatible release; re-evaluate when Tauri ships a `wry` version that pulls in `gtk-rs 0.20`-based webkit2gtk bindings
- [ ] **TODO-0067: Scope or remove `index.html`'s global `button:hover` fallback rule** — `index.html`'s FOUC-prevention `<style>` block contains a project-authored, unscoped `button:hover { background-color: #2563eb; }` rule that matches every native `<button>` in the app by tag name; this rule was masking TODO-0066's `hover:bg-hover` compilation bug (a class-selector hover rule now correctly out-specificities it wherever a real theme-token hover class is present), but it remains a global fallback that could mask future styling bugs the same way and shows an unintended blue flash on any button with no theme-token hover class of its own; audit every button in the app has adequate intentional hover styling, then either scope this rule to only the buttons that actually need it or remove it entirely

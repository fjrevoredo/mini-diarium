# TODO

Open tasks and planned improvements. For full context and implementation notes on the original tasks, see [TODO_EXTRA.md](TODO_EXTRA.md).

TODO entry format:

- `- [ ] **TODO-XXXX: Task title** — concise requirement-style description with scope and constraints`
- Every top-level checkbox item must carry a unique `TODO-XXXX` ID (4-digit zero-padded, e.g. `TODO-0001`)
- Indented sub-items are free-form and do **not** carry IDs
- Write items as requirements/acceptance criteria (what must be true), not implementation plans (how to build it)
- Keep implementation details minimal in TODO entries; move deep implementation notes to [TODO_EXTRA.md](TODO_EXTRA.md) when needed
- New IDs are assigned by reading the `Latest TODO ID` marker near the top of this file and incrementing — never reuse IDs
- After creating a new TODO, update the `Latest TODO ID` marker to reflect the new highest ID
- Use the `todo-manager` skill (`.agents/skills/todo-manager/`) for creation, tracking, archival, and validation

**Latest TODO ID: TODO-0106** — next new TODO should be TODO-0107

---

## High Priority

---

- [x] **TODO-0106: In-app Project Support overlay + streak-triggered header icon** — give users an in-app way to discover how to support Mini Diarium (star, review, share, subscribe, contribute, donate) without ever interrupting the editor. A conditionally-rendered `Heart` header icon invites users who have demonstrably formed a journaling habit (a real `best_streak` milestone at 7/66/365 days, gated on wall-clock journal age so it cannot be triggered by bulk import); a permanent About-screen link invites anyone who goes looking on their own. Both routes open one shared, ascending-friction `ProjectSupportOverlay` checklist. 100% frontend — no backend/Rust changes (`get_statistics()` already returns `best_streak`). Origin: [`docs/explorations/in-app-donation-nudge-proposals.md`](../explorations/in-app-donation-nudge-proposals.md). Full design, milestones, and the UX-GATE walkthrough live in [`docs/archive/project-support-overlay-plan.md`](../archive/project-support-overlay-plan.md).

- [x] **TODO-0105: Cancelling the delete-confirmation dialog restores the entry's real content instead of leaving the editor blank** — TODO-0104 shipped a confirm dialog that blocks navigation away from an entry whose real, on-disk content was just erased in the editor. Clicking Cancel correctly denies navigation and never deletes the entry, but leaves the editor showing the blank state that triggered the dialog, even though the disk row underneath is intact — with no visible way back to the real content short of manually undoing the edit. Acceptance: on Cancel, the editor reloads and displays the entry's actual on-disk content (the exact entry being restored, not just the day's newest, in multi-entry days) before denying the navigation. Full design and TDD task breakdown live in [`docs/entry-persistence-cancel-restore-plan.md`](../entry-persistence-cancel-restore-plan.md).
- [ ] **TODO-0008: Cursor height too tall after Shift+Enter on macOS (#118)** — the text caret (cursor) height becomes extra long starting on the second line, but only when inserting a soft line break with Shift+Enter (Enter alone works fine); macOS-only (v0.4.20); likely a `line-height` or `font-family` mismatch in ProseMirror's `<br>` handling introduced with new editor fonts in v0.4.20; audit soft-break styling in `src/styles/editor.css` and the `--editor-font-family` / `--editor-font-size` CSS custom properties
- [x] **TODO-0104: Entry persistence consent gate — close silent-content-loss gap in soft auto-delete path** — the soft auto-delete path (`delete_entry_if_empty`) treats "this entry never had content" and "this entry had real content that just got erased" identically, deleting both without asking, so a user can silently lose real diary content by erasing it and navigating away. Acceptance: the backend refuses to auto-delete any entry whose on-disk row currently holds real content regardless of what the frontend sends; every navigation path that can reach the user before discarding such an entry shows an in-app confirm dialog first; auto-lock and app-close paths never block on a dialog and rely solely on the backend refusal. Full design, milestones, and the 15-scenario UX-GATE walkthrough live in [`docs/entry-persistence-consent-gate-plan.md`](../entry-persistence-consent-gate-plan.md).
- [ ] **TODO-0091: Integrate SignPath code signing for Windows release artifacts** — the project was accepted into the SignPath Foundation OSS program (acceptance email from Phillip Deng, SignPath); an invitation to the new OSS organization has been sent. Set up the project/artifact configuration (packaged as a zip per SignPath's artifact-configuration docs: https://about.signpath.io/documentation/artifact-configuration/) and the GitHub Actions trusted-build-system integration (https://about.signpath.io/documentation/trusted-build-systems/github), wiring the signing step into `.github/workflows/release.yml` for the Windows installer artifacts. Start with the SignPath-provided self-signed **test** certificate to validate the pipeline end-to-end; production signing only begins after SignPath reviews the setup and imports the real certificate into the organization — do not treat test-cert-signed builds as release-ready. Acceptance: Windows release artifacts are signed via the SignPath GitHub Actions integration using the test certificate, the setup is confirmed working, and `docs/RELEASING.md` documents the new signing step; a follow-up TODO tracks the cutover once SignPath issues the production certificate.
- [x] **TODO-0098: Backup system redesign — first-class local snapshots** — the current system copies `diary.db` on every unlock and keeps 30 by filename order, with no UI, no restore path, no verification, and no time floor in retention. Retention measured in unlock events rather than days is what let users investigating the 0.6.4 data-loss regression overwrite their own pre-incident history; the assessment in [`docs/reports/2026-08-04-backup-system-assessment.md`](../reports/2026-08-04-backup-system-assessment.md) records 17 findings (5 High) and recommends Option A. Requirements: retention guarantees time depth independently of how often the journal is opened; snapshots are taken before risky writes (pending schema migrations, `reset_diary`, journal moves, auth-method removal, import) rather than after them; snapshots are written atomically and verified before being counted; an unchanged journal produces no new snapshot; the user can see backup state and health from the app, including from the unlock screen; and both restore granularities work in-app with no plaintext ever written to disk — whole-journal rollback (taking a safety snapshot first) and per-entry restore from inside a snapshot. Constraints: snapshots stay ordinary encrypted Mini Diarium databases (no new container format, no new cryptography); retention policy stays pure and filesystem access stays behind a narrow boundary so the open-core split and any future tier are not foreclosed; no network in the core. Entry-level revision history is explicitly **out of scope** here and tracked separately as the follow-up option. Acceptance: the five High findings (B-1 to B-5) are closed with tests, `website/docs-src/09-backups.md` is rewritten and the five other stale documentation claims corrected, and a restore is demonstrated end-to-end from a snapshot the app produced itself. Staged delivery, the resolved design decisions, and the UX gate live in [`docs/backup-system-redesign-plan.md`](../backup-system-redesign-plan.md).
  - **Progress (2026-08-04): Milestones 1–2 complete** — snapshot engine (`VACUUM INTO` + fsync + atomic rename + verification, tiered retention, change-counter dedup, storage budget, pre-migration/destructive/lock/exit triggers, manifest sidecar, legacy adoption, background threading, `backup_bench`) and the documentation corrections shipped. Closes findings B-2, B-3, B-4, B-7, B-8, B-10, B-12, B-14, B-15, B-16 and resolves KI-9. **Remaining: Milestones 3–6** — the `backup` command group and Preferences → Backups panel (B-1), read-only inspection (B-6), whole-journal and per-entry restore (B-5), credential-drift and local-only disclosures (B-9, B-11), backups following a journal move (B-13). Milestone 4 is still behind the seven-scenario UX gate.
  - **Progress (2026-08-06): Milestone 3 shipped and reviewed** — the `backup` command group, the Preferences → Backups panel, and the pre-auth **View backups** entry point (B-1). The review in [`docs/reports/2026-08-06-backup-system-redesign-review.md`](../reports/2026-08-06-backup-system-redesign-review.md) raised two findings, both now fixed: backup health could report "Backups are working." while the backups path was blocked by a file or unreadable, and the pre-auth panel disclosed entry counts and date ranges beyond what Task 3.3 authorized. **Milestone 3 stays open**: Task 3.4's Linux `PLATFORM-VERIFY` for `reveal_backups_folder` needs a maintainer on a Linux desktop.
  - **Progress (2026-08-16): Milestones 4–6 closed out.** Milestone 5 (remaining triggers, warnings, documentation) completed. Milestone 6 (cleanup and final verification) completed, including the deferred whole-journal restore rehearsal, which also closes Milestone 4. Task 3.4's Linux-only `PLATFORM-VERIFY` check was split off into its own TODO (TODO-0101) rather than left blocking this TODO indefinitely — it is the one piece of this TODO not resolved by this session, tracked independently. `docs/backup-system-redesign-plan.md`'s Plan Status is `COMPLETED`.
---

## Website Priority

- [ ] **TODO-0011: Website SEO/GEO follow-up backlog** — remaining implementation items from the 2026 website SEO/GEO pass. **The `transition: all` fix is done** (2026-07-16 audit: no `transition: all` remains in `website/css/style.css` — all transitions already use explicit property lists); only the Cloudflare item below remains, and it is a CDN dashboard setting with no repo code change.
  - [ ] **Resolve Cloudflare-injected robots.txt Content-Signal directive** — Cloudflare automatically appends `Content-Signal: search=yes,ai-train=no` to the live robots.txt at the CDN layer; Lighthouse's robots.txt parser flags this as invalid (not part of RFC 9309), costing 8 SEO points (score 92 → 100); the repo `website/robots.txt` is clean — this is a Cloudflare dashboard setting (REPORT.md FIX 2.1)
    - **Fix:** in the Cloudflare dashboard → Security → Bots → Crawler Hints, disable "Content Signals" injection or switch to the HTTP-header equivalent (`X-Robots-Tag: ai-train=no`) if available. No code change in the repo is needed — AI bot blocking is already handled by explicit `User-agent` blocks in the live robots.txt.

---

## Medium Priority

- [ ] **TODO-0050: Update dep-update skills and CI for Nix npmDepsHash** — after PR #159 (Nix flake) merges, update `sync-lockfiles` and `apply-dependency-prs` skills to include the `npmDepsHash` refresh step in `nix/package.nix`, and consider adding a path-filtered CI job to catch stale hashes from contributors who bypass the skills; see [TODO-0050-01](TODO_EXTRA.md#todo-0050-01-skill-and-ci-updates-for-nix-npmdepshash-maintenance) for exact changes. **Part 2 (apply-dependency-prs update) completed 2026-06-29 during the runbook refactor; Part 3 (CI) completed — `.github/workflows/nix.yml` is a path-filtered job (`package-lock.json`/`nix/**`/`flake.*`) that runs `nix build .#default` to verify `npmDepsHash` and even auto-refreshes the hash on push. Only Part 1 (add the `npmDepsHash` refresh step to the `sync-lockfiles` skill) remains.**
- [ ] **TODO-0093: Adopt the orphaned `scripts/*.test.*` files with a `test:scripts` runner** — four build-tooling test files exist and none of them are ever executed: `scripts/enrich-winget-manifest.test.mjs`, `scripts/install-hooks.test.js`, `scripts/sync-skills.test.js`, and `scripts/fingerprint-website-assets.test.mjs` (added by TODO-0088). There is no npm script that runs them and no CI job that invokes them, and `scripts/**` is excluded from Vitest (`vitest.config.ts:16`) and from coverage (line 27), so Vitest will not pick them up either. They are all `node:test` + `node:assert/strict` suites, so a single runner covers the set. Acceptance: a `test:scripts` npm script runs all four (`node --test scripts/`), it is wired into CI and into `bun run pre-commit`, all four suites pass, and the Vitest/coverage exclusions stay as they are so there is no Codecov patch-gate impact.
- [ ] **TODO-0097: Remove the Wayland title-bar workaround once Tauri ships `tao >= 0.36`** — `src-tauri/src/wayland_titlebar.rs` works around [tao#1218](https://github.com/tauri-apps/tao/pull/1218) ([issue #238](https://github.com/fjrevoredo/mini-diarium/issues/238)): tao ≤0.35 wraps its Wayland client-side `GtkHeaderBar` in an `EventBox` with `above_child = true`, which raises an input-only `GdkWindow` over the minimize/maximize/close buttons and swallows every click. Fixed upstream in tao 0.36.0 (2026-07-29) but unreachable while the newest published `tauri-runtime-wry` (2.11.4) pins `tao ^0.35`, so a `[patch.crates-io]` cannot resolve. **Trigger:** the `tao_version_still_needs_the_workaround` guard test in that module starts failing after a Tauri bump. **Acceptance:** Tauri upgraded to the release carrying tao ≥ 0.36; `src-tauri/src/wayland_titlebar.rs`, its `mod` + call site in `lib.rs`, the Linux-only `gtk` dependency in `src-tauri/Cargo.toml`, the guard test, and the `src-tauri/CLAUDE.md` gotcha are all deleted; the CI `xvfb-run` wrapper on the backend-test step is removed if nothing else needs it; re-verified on KDE Plasma 6 / Wayland that the title-bar buttons still respond on first click with stock tao.
- [ ] **TODO-0094: Debug dump for a locked journal** — `generate_debug_dump` requires an unlocked journal, so the most common support case ("I can't unlock") is exactly the one that cannot produce a dump. The data side is largely solved: `db::peek_auth_slot_types(path)` already reads slot types, labels, and the `require_all_auth` flag from a locked journal file without a key, and the app/OS/config/backup/log sections of the dump need no DB handle at all. What is missing is a pre-auth UI entry point — the Advanced tab lives behind the unlock screen. Acceptance: a locked journal can produce a reduced dump (no `content_counts`, no entry stats, no `db_schema_version` beyond what a peek can read), reachable from the unlock screen; the reduced dump passes the same privacy gate as the full one (no key material, no user-chosen string, no path shape), and both docs pages state which fields the reduced form omits.
- [x] **TODO-0102: Fix three docs/tooling drift findings from closing TODO-0098** — three small findings surfaced while closing out TODO-0098's Milestone 6, not part of the backup system itself, and worth their own tracked item since they'd otherwise only live in a now-`COMPLETED` plan file (`docs/backup-system-redesign-plan.md`). (1) `.agents/skills/security-stance/SKILL.md:98` still references `MAX_BACKUPS`, removed from `crates/mini-diarium-core/src/backup/` back in Task 1.3 of the backup redesign (2026-08-04) — correct or remove the reference. (2) `cmd.exe /c "..."` invoked from this agent's Bash tool (Git Bash/MSYS) on Windows silently no-ops: MSYS path-conversion rewrites the literal `/c` argument into a Windows path (`C:\`) before `cmd.exe` sees it, so the command never runs and `cmd.exe` just prints its banner and exits 0 — a false pass indistinguishable from a silently-successful tool like `tsc --noEmit`, caught only because `bun run validate:locales` is not silent on success. Workaround is `MSYS_NO_PATHCONV=1 cmd.exe /c "..."`; root `CLAUDE.md`'s "Commands verified to work from this shell via Windows" list should document this caveat or redirect agents to the PowerShell tool for these commands. (3) The Windows `.cmd`-shim `spawnSync` bug already fixed once in `render-diagrams.mjs` (see its `[0.7.0]` CHANGELOG entry) recurred in `scripts/check-diff-coverage.mjs`'s `generateCoverage()` — now fixed the same way (route through `cmd.exe /d /s /c` on Windows, gated on `IS_WIN`), also with its own `[0.7.0]` CHANGELOG entry. Audit `scripts/*.mjs` and `scripts/*.js` for other `spawnSync`/`execFileSync` calls whose first argument is a bare command name (`bun`, `npm`, `npx`, etc.) rather than routed through a shell, and fix any found. Acceptance: the skill file reference is corrected; the CLAUDE.md Windows execution guidance documents the MSYS gotcha (or redirects to PowerShell); the `scripts/` audit is done and any further instances fixed.
- [x] **TODO-0101: `PLATFORM-VERIFY` — confirm reveal-in-folder on a Linux desktop (Wayland preferred)** — split off from TODO-0098's Milestone 3, Task 3.4 (`docs/backup-system-redesign-plan.md`) because Windows verification is done but Linux verification cannot be performed from a Windows-only machine. Two concrete checks, both against a real Linux desktop (Wayland preferred, given the `tao` title-bar issue tracked in TODO-0097): (1) "Open backups folder" (`reveal_backups_folder` command / `revealItemInDir`) opens the correct directory — the active journal's own `{journal dir}/backups/{db stem}`; (2) it does not open a WebView window, does not navigate the app, and does not trigger focus-loss auto-lock unexpectedly (`src/lib/focus-lock.ts` locks on OS focus loss when `autoLockOnFocusLoss` is enabled). If it does trigger an unexpected lock, treat it as a defect and fix it — the dialog-guard pattern in `src/lib/dialog.ts` is the precedent (root `CLAUDE.md` gotcha 4). Windows result for reference (already recorded in the plan file): Pass — correct directory, no WebView window, no navigation; auto-lock did not fire under a CDP-synthesized click, but Windows blocks synthetic clicks from taking foreground so a real mouse click was not verified there either — note if the same gap applies on Linux. Acceptance: both checks recorded with their outcome in `docs/backup-system-redesign-plan.md` Task 3.4 notes (or, if that plan file is already closed, recorded here and cross-linked), and any defect found is fixed with a regression test.
  - [x] **Linux result (2026-08-16), Fedora 43 / GNOME / Wayland, real mouse click** — Pass on both checks. A release build (`cargo build --release --features custom-protocol`, required to load embedded frontend assets instead of trying the Vite dev server) was run against a sandboxed journal (`MINI_DIARIUM_DATA_DIR`) with one manual snapshot. **Check 1:** a `dbus-monitor --session` capture of the reveal click showed `org.freedesktop.FileManager1.ShowItems` called with `file:///…/backups/diary/backup-2026-08-16-20h15m08.db` — the active journal's own newest snapshot inside its own `backups/{db stem}/` directory, exactly as `reveal_backups_folder` (`src-tauri/src/commands/backup.rs:158-177`) computes it. Nautilus opened to the correct folder with no WebView window and no app navigation. **Check 2:** with `autoLockOnFocusLoss` enabled, a control click into an unrelated window first confirmed the lock mechanism itself fires on this desktop (proving a later result is meaningful); the real click on "Open backups folder" opened Nautilus and the journal **did** lock a few seconds later. This is **not a defect**: leaving the app for Nautilus — an independent, long-lived external program the user may linger in — is the same "genuine departure" case already reasoned through for Windows Explorer in the plan file's Task 3.4 notes ("locking when the user leaves the app for another program is precisely what the setting says it does"). The `src/lib/dialog.ts` guard exists for the app's *own* transient modal native dialogs (open/save/confirm), which return focus as one continuous in-app flow — not for handoffs to a separate application, so extending the guard to `revealBackupsFolder()` was considered and rejected. No code change made.
- [x] **TODO-0103: Restore the default font after inserting a date-time stamp** — after inserting a date-time stamp (and also immediately after pressing Return following a stamp), the caret keeps the stamp's formatting so the next text typed renders in the stamp's font instead of the entry's default font ([issue #261](https://github.com/fjrevoredo/mini-diarium/issues/261)); the reporter offered three options — (1) restore the default font immediately after the stamp, (2) restore it after Return, or (3) render the stamp itself in the default font — and prefers option 3; decide and implement the chosen behavior so text typed after a date-time stamp always renders in the default font. **Implemented option 3**: removed the `.ProseMirror .timestamp` CSS rule (`opacity: 0.7` + `font-size: 0.85em`) in `src/styles/editor.css` — the stamp span now inherits color/size/font straight from the editor body, so it renders identically to surrounding text and the caret-carry-forward mark is visually invisible. Root cause and manual-verification detail confirmed via CDP against the live dev app (computed styles matched exactly before and after Return).

---

## Low Priority / Future
- [ ] **TODO-0096: Analyze feasibility of macOS Mojave (10.14) support** — requested in [issue #241](https://github.com/fjrevoredo/mini-diarium/issues/241). Exploration in that issue established that Mojave is technically reachable (Rust's x86_64 floor is 10.12, and `wry` guards its 10.15+ calls behind runtime version checks) but that the webview is the real blocker: Mojave caps at Safari 14.1.2, while `vite.config.ts` sets `build.target: 'esnext'` and the bundle calls `Array.prototype.at` and `findLast` (both Safari 15.4+), the latter on TipTap's `onUpdate` path. Mavericks (10.9) is impossible — no Rust toolchain targets it. Acceptance: a written decision (ship / defer / decline) backed by a downlevelled trial build, a decision on how to test an OS with no CI runner, and — if declined — the reasoning recorded so the issue can be closed with a real answer. See [TODO-0096-01](TODO_EXTRA.md#todo-0096-01-macos-mojave-support-feasibility-evidence) for the full evidence gathered so far.
- [ ] **TODO-0085: Open-core M4b — record the contribution and relicensing posture** — update `CONTRIBUTING.md` to state that premium features belong in a separate product and repository, and record whether future relicensing of the open core is intended; introduce a CLA only if that recorded decision requires one. Acceptance: contributors can find the product boundary and contribution terms in-repo, the decision is captured in the M4 ADR or a linked ADR, and no CLA is added without an explicit relicensing rationale. Depends on TODO-0084. See [OPEN_CORE_STRATEGY.md §9 step 5 and §10, M4](../OPEN_CORE_STRATEGY.md#10-open-core-extraction-roadmap).
- [ ] **TODO-0086: Open-core M4c — establish the Mini Diarium name-protection posture** — audit the current trademark/name-protection status for "Mini Diarium" and "MiniDiarium+", choose and record the appropriate registration or defensive-use action with jurisdiction and owner, and preserve the distinction between trademark protection and the MIT code license. Acceptance: a dated decision record and evidence/reference to the chosen action exist; any filing or external legal step is explicitly maintainer-approved and tracked without storing confidential details in the repository. Depends on TODO-0084. See [OPEN_CORE_STRATEGY.md §10, M4](../OPEN_CORE_STRATEGY.md#10-open-core-extraction-roadmap).
- [ ] **TODO-0013: Text input extension point** — create a plugin/extension interface for alternative entry methods so official and user plugins can provide text input flows such as dictation, LLM-assisted drafting, and other future capture modes; define capability boundaries, permission model, and how plugins hand content into the editor without weakening the app's privacy guarantees
- [ ] **TODO-0014: Statistics extension point** — add a plugin/extension interface for writing statistics so official and user plugins can calculate custom metrics and surface them in the statistics UI; define the data contract, execution/sandbox constraints, and how custom statistics are registered and rendered without weakening the app's privacy-first local-only model
- [ ] **TODO-0017: Document keypair hex in JS heap** — `generate_keypair` returns `KeypairFiles` with `private_key_hex` as plain JSON so the frontend can write it to a file; add a comment on the struct in `crates/mini-diarium-crypto/src/auth/mod.rs` (where `KeypairFiles` now lives, post-M3a) or `crates/mini-diarium-crypto/src/auth/keypair.rs` (`generate_keypair`) noting this is an accepted design tradeoff and that the private key briefly exists in the JS heap before the file is written
- [ ] **TODO-0018: Sync tool integration** — allow users to point their journal directory at a folder managed by Dropbox, Google Drive, Syncthing, or similar tools; the app should detect when `diary.db` is modified externally while locked (file-system watcher or mtime check on unlock) and prompt the user to reload; document the supported workflow in the UI and guard against opening a partially-synced (in-progress) file; note that the app never initiates any network calls — sync is entirely delegated to the external tool
- [ ] **TODO-0038: Remove legacy `require_all_auth` config migration** — once the release boundary is agreed, remove the legacy `JournalConfig.require_all_auth` field, its migration function `migrate_require_all_auth_to_db`, and all call sites from backend and frontend; the DB-settings-backed implementation that replaced it stays untouched; requires maintainer approval before execution; see TODO-0038-01 for full steps
- [ ] **TODO-0039: Re-evaluate `glib` Dependabot alert when Tauri upgrades webkit2gtk bindings** — Dependabot alert #6 (`glib 0.18.5`, medium) was dismissed as a tolerated risk: the vulnerability is in `glib::VariantStrIter` (Linux-only, UB via unsound iterator impl), the app has zero direct `glib` usage, and upgrading requires `gtk 0.20` + `webkit2gtk 2.1.x` Rust bindings that do not yet exist in a Tauri-compatible release; re-evaluate when Tauri ships a `wry` version that pulls in `gtk-rs 0.20`-based webkit2gtk bindings
- [ ] **TODO-0067: Scope or remove `index.html`'s global `button:hover` fallback rule** — `index.html`'s FOUC-prevention `<style>` block contains a project-authored, unscoped `button:hover { background-color: #2563eb; }` rule that matches every native `<button>` in the app by tag name; this rule was masking TODO-0066's `hover:bg-hover` compilation bug (a class-selector hover rule now correctly out-specificities it wherever a real theme-token hover class is present), but it remains a global fallback that could mask future styling bugs the same way and shows an unintended blue flash on any button with no theme-token hover class of its own; audit every button in the app has adequate intentional hover styling, then either scope this rule to only the buttons that actually need it or remove it entirely
